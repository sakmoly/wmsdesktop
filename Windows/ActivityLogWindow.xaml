<Window x:Class="Wms.Desktop.Windows.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log"
        Height="450"
        Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="Recent sync and application activity (INFO and ERROR)."
                   FontSize="12"
                   Foreground="#6B7280"
                   Margin="0,0,0,8" />
        <ListBox x:Name="LogListBox"
                 Grid.Row="1"
                 Background="White"
                 BorderBrush="#E5E7EB"
                 BorderThickness="1"
                 Padding="8"
                 FontFamily="Consolas"
                 FontSize="11"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
