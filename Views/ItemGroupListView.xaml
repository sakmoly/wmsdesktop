<UserControl x:Class="Wms.Desktop.Views.ItemGroupListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Wms.Desktop.ViewModels"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <viewModels:ItemGroupListViewModel />
    </UserControl.DataContext>

    <Grid Background="#F3F4F6" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Item Groups"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#111827" />
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="Synced from ERPNext (Sync tab or button below). "
                           FontSize="12"
                           Foreground="#6B7280"
                           VerticalAlignment="Center" />
                <Button Content="Sync Item Groups"
                        Command="{Binding SyncItemGroupsCommand}"
                        Padding="10,6"
                        Margin="0,0,8,0" />
                <Button Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Padding="10,6" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="12"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <DataGrid ItemsSource="{Binding ItemGroups}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code"
                                        Binding="{Binding Code}"
                                        Width="180" />
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding Name}"
                                        Width="*" />
                    <DataGridTextColumn Header="Parent Group"
                                        Binding="{Binding ParentGroup}"
                                        Width="180" />
                    <DataGridCheckBoxColumn Header="Is Group"
                                            Binding="{Binding IsGroup}"
                                            Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>


